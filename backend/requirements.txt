# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# LLM + RAG
langchain>=0.3.7
langchain-core>=0.3.17
langchain-groq>=0.2.0
langchain-anthropic>=0.3.0
langchain-openai>=0.2.9
langchain-chroma>=0.1.4
langchain-community>=0.3.7
langchain-text-splitters>=0.3.2
chromadb>=0.5.20

# Sentence transformers (free embedding fallback)
# sentence-transformers removed — using chromadb default (onnxruntime) embeddings instead

# Data / validation
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# HTTP
httpx>=0.28.0

# Env
python-dotenv>=1.0.0
